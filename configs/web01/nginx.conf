user  www-data;
worker_processes  1;
pid /run/nginx-web01.pid;
error_log /home/sysadmin/data-center-lab/srv/web01/logs/error.log warn;
events { worker_connections 512; }
http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    access_log /home/sysadmin/data-center-lab/srv/web01/logs/access.log;
    sendfile on;
    server {
        listen       10.10.10.11:80;
        server_name  web01;
        root         /srv/webcontent;
        index        index.html;
        location /health { return 200 "OK web01\n"; add_header Content-Type text/plain; }
        location / { try_files $uri $uri/ =404; }
    }
}
